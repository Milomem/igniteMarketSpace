import { FormControl, Input as GluestackInput} from '@gluestack-ui/themed'

export function Input({}) {
    return (
        <FormControl mb="$4">
            <GluestackInput>
            </GluestackInput>
        </FormControl>
    )
}